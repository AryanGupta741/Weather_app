import 'package:flutter/material.dart';
import 'package:testing/weather/windeffect/audioplayers.dart';
import 'package:testing/weather/windeffect/windeffect.dart';

class WindScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Stack(
        children: [
          // MyHomePage(), // Wind visual
          // WindSoundEffect(), // Wind sound
        ],
      ),
    );
  }
}
